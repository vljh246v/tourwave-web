---
task_id: T-910
type: feat
created: 2026-04-27T16:25:46Z
owner: (작성자)
---

# T-910 — FE OpenAPI 타입 생성 및 API Client 기반 구축

## 배경 및 목표

gap-matrix.md에서 T-910은 "FE API 클라이언트 fetch 래퍼 (Bearer token, 에러 처리, 재시도)"로 정의된 cross-cutting 태스크다. 핵심 구현(client.ts, errors.ts, schema.ts)은 이미 develop에 존재하지만 두 가지 갭이 남아 있었다: (1) BE T-903에서 추가된 Idempotency-Key 헤더가 반영된 openapi.yaml이 FE openapi/openapi.yaml에 동기화되지 않아 schema.ts가 stale 상태, (2) lib/api/ 모듈에 단위 테스트가 전혀 없음.

## 구현 단계

- [x] 1. 영향 범위 파악 (openapi.yaml drift 53줄, lib/api 테스트 갭 확인)
- [x] 2. `npm run sync:api` 실행 → openapi/openapi.yaml 업데이트 (51 insertions)
- [x] 3. `npm run gen:api` 실행 → schema.ts 재생성 (73 ins / 17 del)
- [x] 4. errors.test.ts 작성 — ApiError 클래스, mapErrorMessage 우선순위 (9개)
- [x] 5. client.test.ts 작성 — Idempotency-Key 자동 주입, 401 핸들러, 204 응답, 에러 매핑 (16개)
- [x] 6. verify-task.sh로 검증

## 영향 범위

| 파일 | 변경 유형 |
|---|---|
| `openapi/openapi.yaml` | 수정 (T-903 Idempotency-Key 4개 도메인 추가 동기화) |
| `src/lib/api/schema.ts` | 재생성 (openapi-typescript auto-generated) |
| `src/lib/api/__tests__/errors.test.ts` | 신규 (단위 테스트 9개) |
| `src/lib/api/__tests__/client.test.ts` | 신규 (단위 테스트 16개) |

## 완료 기준

- [x] 검증기 5개 전부 통과
- [x] 기존 테스트 통과 (39개)
- [x] 신규 단위 테스트 통과 (25개, 총 64개)

## 결정 기록

- openapi.yaml drift: sync + gen만으로 충분. 기존 features/*/api.ts 타입 오류 없음.
- lib/api 테스트: vitest 이미 설정됨. 별도 의존성 추가 불필요.
- client.ts 구현은 이미 Idempotency-Key 자동 주입(UUIDv4), 401 핸들러, 에러 매핑을 포함하고 있어 수정 없이 테스트만 추가.

## 잔재/후속

- gap-matrix.md에서 T-910이 참조된 도메인별 FE API 래퍼(T-108, T-112 등)는 별도 태스크 범위.

## 참고

- ARCHITECTURE.md
- docs/golden-principles.md
- logs/trends/failure-patterns.md
