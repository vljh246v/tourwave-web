---
task_id: T-916
type: feat
status: in-progress
created: 2026-04-18T00:00:00Z
owner: claude
---

# T-916 디자인 시스템 기본 컴포넌트

## 배경 및 목표

Tourwave Web FE에 공통 UI 컴포넌트 라이브러리를 구축한다.
Button, Input, Modal, Card, FormField 5종을 `src/components/ui/` 하위에 구현하고 index.ts로 export한다.

## 구현 단계

- [x] 1. 영향 범위 파악 (globals.css, tsconfig, package.json 확인)
- [x] 2. exec-plan 생성
- [ ] 3. 워크트리 생성 (task-start.sh T-916)
- [ ] 4. Button.tsx — variant(primary/secondary/danger/ghost), size(sm/md/lg), isLoading, forwardRef
- [ ] 5. Input.tsx — label, error, helperText, forwardRef, aria
- [ ] 6. Modal.tsx — isOpen/onClose/title/children/actions, dialog aria, Escape 닫기
- [ ] 7. Card.tsx — header/footer/children/className 슬롯
- [ ] 8. FormField.tsx — Input 래퍼, name/label/type/value/onChange/error/required
- [ ] 9. index.ts — 5개 컴포넌트 re-export
- [ ] 10. verify-task.sh로 검증 (6개 검증기 모두 통과)
- [ ] 11. git commit → 수동 병합

## 영향 범위

- 신규 파일만 (src/components/ui/ 하위)
- src/lib/*, src/features/*, src/lib/auth/ 불변

## 완료 기준

- [x] 6개 파일 생성 (Button, Input, Modal, Card, FormField, index.ts)
- [x] Button 4개 variant
- [x] Input label/error/helperText
- [x] Modal aria + Escape
- [x] npm run typecheck 오류 없음
- [x] npm run build 통과
- [x] verify-task.sh 6개 검증기 모두 PASS

## 참고

- Tailwind v4 CSS-first (@import "tailwindcss" in globals.css)
- React 19 + Next.js 16 App Router ("use client" 필수 for event handlers)
- TypeScript strict mode
